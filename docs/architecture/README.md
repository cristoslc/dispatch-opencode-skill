# docs/architecture/

Expanded component diagrams, protocol flows, and artifact schemas.

Contents:

- **c4-models/** — C4 context, container, and component diagrams (mermaid).
- **protocol-flows/** — Step-by-step dispatch and orchestration flows.
- **artifact-schemas/** — `.subagents/` directory layout, lock file format, FINAL_OUTPUT.md structure.

See `ARCHITECTURE.md` (hub) for the overview.